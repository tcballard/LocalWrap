# Contributing

Generate the Xcode project from `project.yml`; do not commit the generated
`LocalWrap.xcodeproj`. Keep Swift 6 strict concurrency and warnings-as-errors
clean. Before submitting a change, run the native test command from the README,
the deterministic soak, and the ad-hoc universal Release verifier.
